const _  = require('lodash');
const log = console.log;

// Lodash properties

// log('lodash sementic version no', _.VERSION );

// log('template setting ', _.templateSettings)


// (RegExp): Used to detect data property values to be HTML-escaped.
//_.templateSettings.escape
// log(_.templateSettings.escape);


//_.templateSettings.evaluate
// (RegExp): Used to detect code to be evaluated.
// log(_.templateSettings.evaluate);



// _.templateSettings.imports
// (Object): Used to import variables into the compiled template.
// log(_.templateSettings.imports);



// _.templateSettings.interpolate
// Used to detect data property values to inject.
// log(_.templateSettings.interpolate)


// _.templateSettings.variable
//(string): Used to reference the data object in the template text
// log(_.templateSettings.variable)


//methods
// _.templateSettings.imports._

// log(_.templateSettings.imports._)